import React from 'react'
import PropTypes from 'prop-types'

function UpdateTasks(props) {
    return (
        <div>
            <button name="updateTasks" color="red" onClick={onClick()}>Update tasks</button>
        </div>
    )
}

UpdateTasks.propTypes = {

}

function onClick(){
    
}

export default UpdateTasks

